# Copy to .env.local for local dev, and set the same key in Vercel's
# Project Settings -> Environment Variables for production.
# Leave unset and analytics simply never loads (see src/lib/analytics.ts).
VITE_GA_MEASUREMENT_ID=
